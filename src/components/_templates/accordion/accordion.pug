mixin accordion-list(label,title, text)
  .accordion.js-openAccordion
    .accordion--button
      span.label=label
      p.accordion--button_text=title
      span.icon
    .accordion--content.is_cascade
      p=text
      p=text

